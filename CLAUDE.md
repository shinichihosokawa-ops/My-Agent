# KAIB 会費徴収・照合・領収書自動発行システム

## システム概要
一般社団法人香川イノベーションベース（KAIB）の会費徴収を管理するシステム。
Google Form → Google Sheets → 入金照合 → 領収書PDF生成 → Gmail送信 を自動化。

## 会員区分と料金
- **正会員**: 入会金 30,000円 + 年会費 120,000円 = 合計 150,000円
- **準会員**: 入会金なし + 年会費 70,000円 = 合計 70,000円
- **学生会員**: 入会金なし + 年会費 30,000円 = 合計 30,000円

## 振込先
SMBC TRUNK（法人口座）

## Google Sheets構成

### シート1: Form Responses 1（読み取り専用）
Google Formの回答が自動記録される。
| 列 | 内容 |
|----|------|
| A | タイムスタンプ |
| B | メールアドレス |
| C | 氏名（漢字） |
| D | 振込口座名義（カタカナ） |
| E | 領収書宛名 |
| F | 会員区分 |
| G | 振込予定日 |
| H | フォーラム参加希望 |
| I | メンタリング参加希望 |
| J | 御社名（学生の場合は学校名） |
| K | FacebookアカウントURL |

### シート2: 顧客管理データ
- A〜K列: `=QUERY('Form Responses 1'!A:K, "SELECT *", 1)` でフォーム回答を自動同期
- **L列: 入金日** — 手動で入金日を記入（入金確認の印）
- **M列: 領収書送付完了** — システムが自動で「◯ (領収書番号)」を記入

### シート3: 処理済み
スクショ方式で処理した入金の重複防止記録。
| A | B | C | D | E |
|---|---|---|---|---|
| メールアドレス | 照会番号 | 領収書番号 | 処理日時 | ステータス |

## 入金管理の2つの方法

### 方法1: スクショ方式（自動照合）
1. 銀行の入金スクリーンショットをGoogle Driveフォルダにアップロード
2. `npm run process` 実行
3. Claude APIが画像を解析して入金データを抽出
4. カタカナ名義で会員と照合
5. マッチした会員に領収書PDFを生成・メール送信
6. スクショは処理済みフォルダに移動

### 方法2: 手動確認方式
1. 顧客管理データのL列に入金日を記入
2. `npm run process` 実行
3. L列に入金日あり + M列が空の会員に領収書を自動送信

### 重複防止
- M列に「◯」がある会員は両方式とも処理対象外
- 処理済みシートで照会番号の重複チェック（スクショ方式）

## 領収書PDF生成 (`src/receipt.ts`)

### 構成要素
- **タイトル**: 「領 収 書」
- **領収書番号**: `RCP-YYYYMMDD-XXX`（日付+ランダム3桁）
- **発行日**: 入金日（overrideDateパラメータ）
- **宛名**: 会員のreceiptAddress + 「様」
- **金額**: 会員区分に対応する合計金額
- **但し書き**: 入会金ありの場合は「入会金および年会費の代理受領分として」、なしの場合は「年会費の代理受領分として」
- **内訳**: 入会金・年会費・合計の明細と対象期間
- **発行元**: 株式会社HOSOKAWA（KAIB設立準備事務局 代理）
- **社印**: `assets/seal.png` — sharpで150x150にリサイズしてPNG変換後に埋め込み

### 技術詳細
- pdf-lib + fontkit@1.9.0 で日本語対応
- フォント: `fonts/NotoSansJP-Regular.ttf`（全テキストに使用）
- fontkit は `require('fontkit')` で読み込み（@pdf-lib/fontkitではない）
- 社印画像はsharpで圧縮してからembedPng

## メール送信 (`src/gmail.ts`)
- **件名**: 【領収書】KAIB年会費のお支払いありがとうございます（{領収書番号}）
- **本文**: お礼文 + 領収書PDF添付
- Gmail API (OAuth2) で送信

## 運用コマンド
- `npm run process` — メイン処理（手動確認分 + スクショ分を一括処理）
- `npm run auth` — Google OAuth2トークン取得/更新
- `npx ts-node src/test-email.ts` — テスト用領収書メール送信

## 必要なファイル
- `.env` — 各種APIキー・トークン・スプレッドシートID
- `fonts/NotoSansJP-Regular.ttf` — 日本語フォント
- `assets/seal.png` — 社印画像（JPEG形式でも可、sharpが変換）

## デプロイ
Google Cloud Functions (asia-northeast1) にHTTPトリガーでデプロイ可能:
```
npm run deploy
```
